# CS320: Project 2
## Basic Information
__Name__: Renze Chen  
__BU-ID__: rchen102  
__B-Number__: B00767193  

## Running
Following are the commands and the instructions to run program

1. __Instruction to compile__
   ```shell
   make
   ```
   Compiles the code and generate `predictors` executable file

2. __Instruction to run__
   ```shell
   make run
   ```
   Default: execute the command `./predictors short_trace1.txt output.txt`  
   For different input: goto `Makefile` and change the file name

3. __Instruction to clean__
   ```shell
   make clean
   ```
   Clean up all the `.o`,`.exe` files.
